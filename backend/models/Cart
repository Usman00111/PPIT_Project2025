const mongoose = require("mongoose");

// starting with an empty schema for now
const cartSchema = new mongoose.Schema({}, { timestamps: true });

module.exports = mongoose.model("Cart", cartSchema);
